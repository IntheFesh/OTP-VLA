# =============================================================================
# §V.D ablation: V3 baseline + drop proprio (cell G)
# Seed: 2026
# Generated by _gen_ablation_configs.py on 2026-05-13 (Day 5)
# Inherits from otp_soft_frozen.yaml; overrides lr=1e-4 per Path B convention.
# Estimated wall clock: ~12.5 hr per seed on RTX PRO 6000.
# =============================================================================

defaults:
  - otp_soft_frozen
  - _self_

seed: 2026

model:
  otp_head:
    deterministic: false
  decoder:
    drop_proprio: true

train:
  lr: 1.0e-4
  num_epochs: 50
  num_steps: null
  batch_size: 16
  log_every: 50
  ckpt_every: 5000
  warmup_steps: 500
  grad_clip: 1.0

data:
  train_end_demo: 45

output_dir: results/ablation_drop_proprio_seed2026_${now:%Y%m%d_%H%M%S}

wandb:
  enabled: false
  project: otp-vla
  run_name: ablation_drop_proprio_seed2026
