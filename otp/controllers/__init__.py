"""otp.controllers — placeholder for future control modules.

The fixed-φ Stage 2 controller (FixedManipulationController + components)
has been deprecated; the snapshot is preserved on the `otp-fixed-phi-validation`
branch as the empirical anchor referenced in the paper §III.

The `otp-soft-implementation` branch replaces it with a learned
LanguageAgnosticDecoder (otp/models/language_agnostic_decoder.py).
"""
