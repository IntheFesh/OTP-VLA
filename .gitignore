# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/

# Backup files
*.bak

# Third-party clones — too large, anyone can re-clone
third_party/

# Data — too large for git, regenerate with scripts/
# Anchored to repo root so it does NOT shadow otp/data/ source files.
/data/

# Results — keep in branch but exclude large binary artifacts
# (We DO want gt_replay_smoke*.json in this branch as evidence)

# Environments
.venv/
venv/
.conda/

# Editor
.vscode/
.idea/
*.swp
logs/
results/
outputs/

# Local pretest / inspection logs (per-machine, not shared)
*_output.txt
inspection_output.txt
pretest_*.txt
*.bak

# v3 split-introduction backups (kept locally as safety net)
*.bak_v3split
results/smoke_split_*/
pretest_*.log

# Backup files from any patch session
*.bak_*

# Local pointers (run-specific, not for repo)
.last_train_pid
.last_train_run
.paper_ready_ckpt
.last_diag_pid
.last_diag_run
