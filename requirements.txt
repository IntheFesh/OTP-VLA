# ============================================================
# OTP-VLA Requirements
# Python 3.10, CUDA 12.1+, PyTorch 2.4+
# ============================================================

# --- Core deep learning ---
torch==2.4.0
torchvision==0.19.0

# --- Transformers / fine-tuning ---
transformers==4.40.1
accelerate==0.32.1
peft==0.11.1
bitsandbytes==0.43.1

# NOTE: flash-attn must be installed separately with --no-build-isolation:
#   pip install flash-attn==2.5.5 --no-build-isolation
# flash-attn==2.5.5

# --- TensorFlow (OpenVLA-OFT dataset pipeline) ---
tensorflow==2.15.0
tensorflow-datasets==4.9.3

# --- OpenVLA-OFT custom dataloader ---
dlimp @ git+https://github.com/moojink/dlimp_openvla.git

# --- Utilities ---
einops>=0.7.0
hydra-core>=1.3.2
omegaconf>=2.3.0
wandb>=0.17.0

# --- Vision / media ---
imageio>=2.34.0
opencv-python>=4.9.0

# --- Data IO ---
h5py>=3.10.0
pyyaml>=6.0

# --- Plotting / analysis ---
matplotlib>=3.8.0
seaborn>=0.13.0
pandas>=2.2.0
scipy>=1.13.0
tqdm>=4.66.0

# --- Testing ---
pytest>=8.0.0
pytest-cov>=5.0.0

# --- Notebooks ---
jupyter>=1.0.0

# --- LLM providers (counterfactual instruction generation) ---
openai>=1.30.0
anthropic>=0.30.0
